type NativeArticleData = {
    articleInfo: ArticleInfo,
    rendered: string,
};
type ArticleRouterData = {
    title: string,
    tags: string[],
    author: string,
    created: string,
    lastCommit: CommitInfo,
    rendered: string,
    id: string
}
